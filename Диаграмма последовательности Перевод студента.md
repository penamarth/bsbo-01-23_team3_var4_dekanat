```
@startuml
actor "Сотрудник деканата" as DeanWorker
participant "DeanOfficeController" as DO
participant "IEducationalDepartmentService" as IEDS
participant "IGroupsService" as IGS
participant "IStudentsRepository" as ISR

DeanWorker -> DO: TransferStudent(studentId, groupId)
activate DO

DO -> IEDS: CheckRequestStatus(studentId)
activate IEDS
IEDS --> DO: RequestStatus
deactivate IEDS

DO -> IGS: TransferStudent(studentId, groupId)

activate IGS

IGS -> ISR: GetById(studentId)
activate ISR
ISR --> IGS: Student
IGS -> ISR: UpdateStudent(student)
ISR --> IGS: Студент успешно удалён из группы
deactivate ISR

IGS -> ISR: GetById(studentId)
activate ISR
ISR --> IGS: Student
IGS -> ISR: UpdateStudent(student)
ISR --> IGS: Студент успешно добавлен в группу
deactivate ISR

IGS --> DO

deactivate IGS

DO --> DeanWorker: Студент успешно переведён
deactivate DO
@enduml
```